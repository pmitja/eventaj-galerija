import { SolutionRoute, solutionMetadata } from "@/components/landing/solution-route";

export const metadata = solutionMetadata("en", "event-qr-gallery");
export default function Page() { return <SolutionRoute locale="en" id="event-qr-gallery" />; }
