import { SolutionRoute, solutionMetadata } from "@/components/landing/solution-route";

export const metadata = solutionMetadata("nl", "event-qr-gallery");
export default function Page() { return <SolutionRoute locale="nl" id="event-qr-gallery" />; }
